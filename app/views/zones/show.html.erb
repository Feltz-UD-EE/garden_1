<p style="color: green"><%= notice %></p>

<%= render @zone %>

<div>
  <%= link_to "Edit this zone", edit_zone_path(@zone) %> |
  <%= link_to "Back to home", public_home_path %>

  <%= button_to "Destroy this zone", @zone, method: :delete %>
</div>
