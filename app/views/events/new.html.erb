<h1>New event</h1>
<p>Crop: <%= @crop.name %> (<%= @crop.description %>) in zone <%= @crop.zone.name %></p>
<%= render "form", event: @event %>

<br>

<div>
  <%= link_to "Back to events", events_path %>
</div>
